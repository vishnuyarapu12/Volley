# Copy this file to .env and fill in your values

# Backend base URL (no trailing slash)
# Leave empty in local dev — Vite proxy handles /api → localhost:5000
# Example for production: https://your-backend.onrender.com
VITE_API_URL=
